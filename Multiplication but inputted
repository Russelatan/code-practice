#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n1,t,i;
    int x = 1;

    printf("Enter a number: ");
    scanf("%d",&i);

    while(x <= 10){
        t=x*i;
        printf("%d * %d = %d\n",i,n1,t);
        x++;

    }

    return 0;
}
